local typedefs = require "kong.db.schema.typedefs"

return {
    name = "test",
    fields = {
        {
            config = {
                type = "record",
                fields = {

                },
            },
        },
    }
}